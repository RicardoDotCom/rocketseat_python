# No python a identação é um conceito muito importante pois é desta forma que são discriminados os blocos de código, diferente de outras linguagem que utilizam () ou {}
# Variavel é o que definimos para armazenar dados de forma simples. 
# São de diferentes tipos

# Regras: 
    # As variaveis devem ser nomeadas;
    # Devem iniciar com letras não podendo ser iniciadas por Números, caracteres especiais ou simbolos;
    # Diferencia entre maiusculas e minusculas;
    # Para atribuir um valor devemos usar o igual =; 
    # No Python não preciso informar o tipo da variavel antes de declarar diferente de outras linguagens;
    # E a tipagem é dinamica podendo receber qualquer tipo de dado;
    # Por boas práticas as variaveis, funções e métodos devem ser descritas usando o snake_case, separando as palavras com um UNDERLINE
    # Já classes devemos utilizar o camelCase para nomea-las;
    # O guia de boas praticas é o pep 8 (pep heith) e pode ser consultado on-line e de graça

nome_completo = "Ricardo Andrade" 
idade = 25

